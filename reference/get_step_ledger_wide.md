# Wide ledger for one standard

Wide ledger for one standard

## Usage

``` r
get_step_ledger_wide(standard = "data_category")
```

## Arguments

- standard:

  Character. The standard to pivot wide. Default: "data_category".
