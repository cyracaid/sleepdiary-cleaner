# Regenerate the figure_index.png contact sheet

Regenerate the figure_index.png contact sheet

## Usage

``` r
run_figure_index(viz_dir = "latest_visualization")
```

## Arguments

- viz_dir:

  Character. Path to the figure run directory. Default
  `"latest_visualization"`.

## Value

Invisibly TRUE.
