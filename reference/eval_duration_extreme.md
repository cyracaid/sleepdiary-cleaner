# Evaluate duration_extreme (separate from severity count)

Evaluate duration_extreme (separate from severity count)

## Usage

``` r
eval_duration_extreme(df, cfg = NULL)
```

## Arguments

- df:

  A data frame with sleep_duration_h column.

- cfg:

  Pipeline configuration list.
