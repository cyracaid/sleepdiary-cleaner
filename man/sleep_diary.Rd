% Generated by hand — corresponds to R/sleep_diary.R
\name{sleep_diary}
\alias{sleep_diary}
\alias{new_sleep_diary}
\alias{as_sleep_diary}
\alias{is_sleep_diary}
\alias{validate_sleep_diary}
\title{The sleep\_diary S3 class}
\description{
  A thin container that carries the working data frame plus the provenance
  of every pipeline step that has touched it.  Each step wraps its
  unchanged v1.2.0 logic, only adding timing, column-diff tracking and
  ledger logging.
}
\usage{
new_sleep_diary(data, step_id = "0", step_label = "raw",
                cfg = NULL, history = list(), extra = list())
as_sleep_diary(x, ...)
is_sleep_diary(x)
validate_sleep_diary(x)
}
\arguments{
  \item{data}{A data frame.}
  \item{step_id, step_label}{Step identification recorded in provenance.}
  \item{cfg}{Pipeline configuration list, or NULL.}
  \item{history}{List of prior step records.}
  \item{extra}{Optional extra fields merged into the step record.}
  \item{x}{An object.}
  \item{...}{Additional arguments passed through.}
}
\value{
  \code{new_sleep_diary()} and \code{as_sleep_diary()} return a
  \code{sleep_diary} object.
  \code{is_sleep_diary()} returns a logical scalar.
  \code{validate_sleep_diary()} returns \code{x} invisibly or raises an error.
}
