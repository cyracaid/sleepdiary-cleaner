```
# 查看每列的非NA数量
library(dplyr)

summary_stats <- full_df %>%
  select(all_of(issues_cols)) %>%
  summarise(across(everything(), 
                  list(
                    non_na = ~sum(!is.na(.)),
                    percent = ~mean(!is.na(.)) * 100
                  )))

print(summary_stats)

# 或者更简单的查看方式
sapply(full_df[issues_cols], function(x) sum(!is.na(x)))
```



```
# 查看所有非空的评论内容
comments_data <- full_df %>%
  select(pid, date_of_obs, comments_am, survey_comments, survey_concerns) %>%
  filter(
    (!is.na(comments_am) & comments_am != "") |
    (!is.na(survey_comments) & survey_comments != "") |
    (!is.na(survey_concerns) & survey_concerns != "")
  ) %>%
  head(10)

# 打印出来，确保能看到完整的文本内容
options(max.print = 1000)  # 增加打印限制
print(comments_data)

# 如果想看得更清楚，可以逐行查看
for(i in 1:nrow(comments_data)) {
  cat("\n--- 行", i, "---\n")
  cat("PID:", comments_data$pid[i], "\n")
  cat("日期:", comments_data$date_of_obs[i], "\n")
  cat("早晨评论:", comments_data$comments_am[i], "\n")
  cat("调查评论:", comments_data$survey_comments[i], "\n")
  cat("关注点:", comments_data$survey_concerns[i], "\n")
}
```

```
# 计算有多少比例是同一天完成的
full_df %>%
  mutate(
    start_date = as.Date(StartDate),
    end_date = as.Date(EndDate),
    same_day = start_date == end_date
  ) %>%
  summarise(
    total_rows = n(),
    same_day_count = sum(same_day, na.rm = TRUE),
    same_day_percent = mean(same_day, na.rm = TRUE) * 100
  )
  
    total_rows same_day_count same_day_percent
1      13990          13928         99.55683
```



根据列名分析，以下是 `full_df` 中所有与日期/时间相关的列：

## 1. 主要的日期时间列
```r
# 主要的调查开始和结束时间
main_datetime_cols <- c(
  "StartDate",      # 调查开始时间
  "EndDate",        # 调查结束时间
  "RecordedDate",   # 记录日期
  "date_of_obs",    # 观察日期
  "ts"              # 时间戳
)
```

## 2. 上午问卷(AM)的时间相关列
```r
am_time_cols <- c(
  "time_bed_am_hhmm",        # 上床时间
  "time_bed_am_ampm",        # 上床时间 AM/PM
  "time_sleep_am_hhmm",      # 入睡时间
  "time_sleep_am_ampm",      # 入睡时间 AM/PM
  "duration_sol_estimate_am_hhmm",  # 入睡所需时间
  "time_awake_am_hhmm",      # 醒来时间
  "time_awake_am_ampm",      # 醒来时间 AM/PM
  "time_getup_am_hhmm",      # 起床时间
  "time_getup_am_ampm"       # 起床时间 AM/PM
)
```

## 3. 下午问卷(PM)的时间相关列
```r
pm_time_cols <- c(
  "caffeinetoday_PM_hhmm",           # 咖啡因摄入时间
  "caffeinetoday_PM_ampm",           # 咖啡因摄入时间 AM/PM
  "alcoholtoday_PM_hhmm",            # 酒精摄入时间
  "alcoholtoday_PM_ampm",            # 酒精摄入时间 AM/PM
  "nicotine_amount_pm_hhmm",         # 尼古丁使用时间
  "nicotine_amount_pm_ampm",         # 尼古丁使用时间 AM/PM
  "cannabis_amount_pm_hhmm",         # 大麻使用时间
  "cannabis_amount_pm_ampm",         # 大麻使用时间 AM/PM
  "duration_totalmin_napstoday_PM"   # 小睡总时长（分钟）
)
```

## 4. 药物服用时间
```r
medicines_time_cols <- c(
  "medicines_1_time",
  "medicines_2_time",
  "medicines_3_time",
  "medicines_4_time",
  "medicines_5_time",
  "medicines_6_time",
  "medicines_7_time",
  "medicines_8_time",
  "medicines_9_time",
  "medicines_10_time"
)
```

## 5. 其他时间相关列
```r
other_time_cols <- c(
  "acti_time_remove",      # 活动记录移除时间
  "vital_time_remove",     # 生命体征移除时间
  "date_id",               # 日期ID
  "day_num"                # 天数编号
)
```

## 6. 计时器相关列（记录页面停留时间）
```r
timer_cols <- grep("_timer_", names(full_df), value = TRUE)
```

## 查看所有日期时间列
```r
# 合并所有日期时间列
all_datetime_cols <- c(
  main_datetime_cols,
  am_time_cols,
  pm_time_cols,
  medicines_time_cols,
  other_time_cols,
  timer_cols
)

# 显示所有日期时间列
cat("=== 所有日期/时间相关列 ===\n")
print(all_datetime_cols)
cat("\n总共找到", length(all_datetime_cols), "个日期/时间相关列\n")
```

## 检查这些列的数据类型
```r
# 查看这些列的数据类型
datetime_cols_types <- full_df %>%
  select(any_of(all_datetime_cols)) %>%
  summarise(across(everything(), ~class(.x)[1]))

print(datetime_cols_types)
```

## 查看样本数据
```r
# 查看前几行的主要日期时间列
full_df %>%
  select(any_of(c(main_datetime_cols, "pid"))) %>%
  head()
```

主要的日期时间列是 `StartDate` 和 `EndDate`，其他大多是记录具体事件时间的列（如服药时间、咖啡因摄入时间等）。





```
full_df %>%
  select(all_of(main_datetime_cols)) %>%
  na.omit() %>%  # 删除任何包含 NA 的行
  head(10)
```



```
> cat("df 行数:", nrow(df), "\n")
df 行数: 13990 
> cat("full_df 行数:", nrow(full_df), "\n")
full_df 行数: 13990 
> 
> # 检查pid的对应关系
> pid_check <- data.frame(
+     df_pid = df$pid,
+     full_df_pid = full_df$pid,
+     row_number = 1:nrow(df)
+ )
> 
> # 查看pid不匹配的行
> pid_mismatch <- pid_check %>%
+     filter(df_pid != full_df_pid)
> 
> if(nrow(pid_mismatch) > 0) {
+     cat("警告：发现", nrow(pid_mismatch), "行pid不匹配\n")
+     print(head(pid_mismatch))
+ } else {
+     cat("所有行的pid都匹配！\n")
+ }
所有行的pid都匹配！
> 
> # 如果pid都匹配，可以直接按行对应添加
> if(nrow(pid_mismatch) == 0) {
+     df$StartDate <- full_df$StartDate
+     cat("成功添加 StartDate 列\n")
+ }
成功添加 StartDate 列
```



"avg_waso_estimate_am_minutes" %in% colnames(full_df)

"num_waso_estimate_am" %in% colnames(full_df)

```
> "avg_waso_estimate_am_minutes" %in% colnames(full_df)
[1] FALSE
> "num_waso_estimate_am" %in% colnames(full_df)
[1] TRUE
```

```
pid 2854
day_num 13
row_id 2303
比如这种 还有可能被误解为小睡的时间，因为他在床上时间醒来时间总共相差可能也就不过半个小时这样子的，而且也是在白天，而且这个人他平时睡觉的时间一般都在晚上凌晨就是他上床的时间，然后这边上床的时间是早上10:00，然后我在想有没有可能他是把它理解为num_waso.但是鉴于这个人的其他十来条记录对于num_waso理解看上去是正确，没有别的错误，我感觉最好的还是把这一条给丢掉。

```

