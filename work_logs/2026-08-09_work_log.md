# 工作日志 — 2026-08-09

**主题：** 数据字典审计定案 —— v1.4 交付规格的最终决策

对照代码：`splsleep` commit `610572f`
审计文档：`work_logs/open_issues_2026-08-07.md`
参照实现：`R01_online_sleepdiary_manualclean` 的 `calculate_sleep_time_vars.R`

---

## 一、核心决策：`sleeponset` 不加自报 SOL

### 结论

```r
self_diffcalc_sleeponset = time_sleep_corrected        # 保持现状，不加 SOL
```

**splsleep 当前实现（`calculate_sleep_time_end.R:131`）是正确的。参照实现在这一行有误。**

### 争议是什么

参照实现（`calculate_sleep_time_vars.R` 第 49 行）：

```r
self_diffcalc_sleeponset = lubridate::minutes(duration_totalmin_sol_estimate_am_mincalc)
                           + time_sleep_am_hhmm_ampm
```

即 **sleeponset = time_sleep + 自报SOL**。

splsleep 某次改动把它改成了直接取 `time_sleep_corrected`，注释理由是
`"should not be added here or SOL gets double-counted"`。**这次改动此前没有任何文档记录**，
是本轮审计对照截图才发现的。

### 判断依据

**依据一：`SCHEMA.md:13` 的定义**

```
| `time_sleep_hhmm` | `time_sleep_am_hhmm` | character | Sleep-onset HH:MM |
```

`time_sleep` 被定义为 **Sleep-onset（入睡时刻）**，不是「上床准备睡」。

**依据二：参照实现自身存在内部矛盾**

同一份参照实现，第 47 行：

```r
self_diffcalc_sol_minutes = difftime(time_sleep_am, time_bed_am)   # SOL = 睡着 − 上床
```

这一行把 `time_sleep − time_bed` 当作 SOL，**说明它也认为 `time_sleep` 是入睡时刻**。
但第 49 行又给 `time_sleep` 加了一次自报 SOL。

**同一段延迟被计入两次**：一次隐含在 `time_sleep` 的位置里（它本就在 `time_bed` 之后 SOL 那么久），
一次显式加上去。第 47 行与第 49 行对 `time_sleep` 的假设不一致。

**依据三：加 SOL 后的量没有物理对应**

`time_sleep + 自报SOL` = 「睡着之后又过了约 29 分钟」的那个时刻。
日记里不存在这个事件。

### 数值影响

若改为加 SOL：sleeponset 后移 → `sleepperiod = awake − sleeponset` 变短 → **TST 变短同样多**。

按真实数据 Mean SOL = 28.8 min 估算：

| | Mean TST |
|---|---|
| 当前（不加 SOL） | **7.71 h** |
| 若加 SOL | 约 7.23 h |

**差约 29 分钟，系统性、方向单一。** 这个偏移会直接进入论文的主要结局指标。

### 待确认（外部依赖）

**本决策的唯一薄弱点：没有人核对过问卷原题。**

`SCHEMA.md` 的定义是本仓库的二手描述，不是问卷本身。需要向 Maia 确认一句话：

> 睡眠日记里那道题的原文是什么？问的是「你几点睡着的」还是「你几点上床准备睡 / 熄灯」？

**若原题是「几点上床准备睡」，则以下三件事要一起改，不能只改一件：**

1. `sleeponset` 恢复 `+ SOL`
2. `self_diffcalc_sol_minutes = time_sleep − time_bed` 是错的（那测的是上床→熄灯，不是 SOL）
3. `SCHEMA.md:13` 的定义要改

**在回音之前，本决策按「几点睡着」执行，且 `tst_minutes` 交付时应附注此前提。**

---

## 二、连带发现：`sleepperiod` 与 `trysleep` 按构造相同

### 事实

决定不加 SOL 之后，两个量在代码层面完全等价：

```r
sleeponset  = time_sleep_corrected                            # 131 行
sleepperiod = time_awake_corrected − sleeponset               # 167 行
            = time_awake_corrected − time_sleep_corrected
trysleep    = time_awake_corrected − time_sleep_corrected     # 137 行
```

**不是数值接近，是同一个表达式。** 参照实现里两者不同，正因为它的 sleeponset 加了 SOL。

验证：

```r
d <- readRDS("output/corrected_ema_data.rds")
all.equal(d$self_diffcalc_sleepperiod_minutes, d$self_diffcalc_totaltrysleep_minutes)
```

### 曾考虑并否决的做法

**否决：为了让两列不重复，而给 sleeponset 加回 SOL。**

否决理由：**这是拿算法迁就列表。** 列重复是呈现层的问题，
加 SOL 会改变一个物理量的定义并使 TST 系统性偏移约 29 分钟。
用一个会影响主要结局的算法改动，去解决一个「表里有两列一样」的整洁性问题，
代价与收益完全不成比例。

**这条记下来，是因为它看起来很有诱惑力，且理由听上去合理（"避免冗余"）。**

### 采纳的做法

**Dataset A 只保留 `sleepperiod_minutes`，删去 `trysleep_minutes`。**

保留 `sleepperiod_minutes` 而非 `trysleep_minutes` 的理由：

1. 它是 TST 公式的直接输入（192 行）
2. 它是决策 B2 选定的「TST 上界」
3. 名字更贴近它在本数据集中的实际含义

### 应写入 Methods 的一句话

> 本日记未采集夜间中途醒来的时刻，因此「睡眠期」与「尝试入睡期」在本数据集中不可区分，
> 二者按构造相同。

**这是数据设计的事实，不是缺陷，应当明写而非掩盖。**

---

## 三、本轮全部决策

| # | 议题 | 决定 | 影响 |
|---|------|------|------|
| **B1** | `sleeponset` 是否加 SOL | **不加**（`time_sleep` 即入睡时刻） | 代码无需改动；待 Maia 确认问卷原题 |
| **B2** | TST 因缺 WASO 而 NA（约 1,127 条） | **选 C**：字典标注 `sleepperiod_minutes` 为 TST 上界 | 零代码改动；记录不丢失 |
| **S1** | 计算侧 WASO 缺席 Dataset A | `awake_getup_diff_h × 60` → **`waso_computed_minutes`** | A 加一列；解决单位不一致（原为小时） |
| **S2** | Figure 20B 命名误导 | 标题改为 **"Self-Reported Nighttime Wakefulness vs Post-Awakening Time in Bed"** | 消除「同一量两种测法」的暗示 |
| **S4** | Dataset B 主键不唯一 | 加 **`row_id`** | 与 A 同主键，消除多对多风险 |
| **M6** | B 无法自足 | 加 **`correction_type`** | B 变 15 列，「改了多少」与「用什么规则改」同表 |
| **M2** | 命名不对称 | `sol_minutes` → **`sol_computed_minutes`** | 与 `sol_selfreport_minutes` 成对 |
| **M3** | 自报派生量名字不明 | `waso_avg_minutes` → **`waso_avg_bout_selfreport_minutes`** | 明示分子分母皆自报 |
| **新** | `sleepperiod` ≡ `trysleep` | **删 `trysleep_minutes`** | 见 §二 |

**Dataset A：36 列**（原 36 + `waso_computed_minutes` − `trysleep_minutes`）
**Dataset B：15 列**（原 13 + `row_id` + `correction_type`）

---

## 四、重要背景：Dataset A / B 目前尚不存在

审计中确认（两个仓库、所有分支、所有 `.R` 文件）：

| 搜索目标 | `.R` 文件中命中 | 仅出现在 |
|---|---|---|
| `finalize_columns` | **0** | — |
| `record_status` | **0** | `.md` 文档 |
| `cleaned_data_final` | **0** | `.md` 文档 |
| `cleaned_data_prepostcorrection` | **0** | `.md` 文档 |

`output/` 中也没有对应文件。

**`data_dictionary.md` 是一份规格说明，不是对已有产出的描述。**

这不是问题——已完成的是把 137 列收敛到 36 + 15 列的**全部决策工作**
（`column_review`、双 agent 辩论、`two_dataset_delivery_plan`、`data_dictionary`），
没有这些，`finalize_columns()` 无从写起。

**缺的是最后一步：把规格翻译成 R 代码。**

**这个时机反而是最好的：** 上述所有改名与增删都只需改文档，
没有迁移成本、没有向后兼容负担、没有「已发出的数据要不要召回」。

---

## 五、下一步

### 实施顺序

1. **把 `data_dictionary.md` 从 `proj_splclean` 同步进 `splsleep`**
   —— 规格在一个仓库、代码在另一个仓库，是 08-05 丢失修复的同一个坑
2. **字典转 `inst/extdata/column_dictionary.csv`**
   —— 一张表同时承担白名单、改名映射、数据字典三个角色。
   带 `transform` 列处理 S1 的 `×60`，避免单位换算散落在代码里
3. **写 `R/finalize_columns.R`**，含两条断言：
   - 字典标 A/B 的列必须都在 data 中 → 缺失即 `stop()`（交付物少列必须炸）
   - data 中管线产生的列必须都在字典中 → 缺失即 `warning()`（新列要可见，但不阻断）
4. **三条测试：**
   - A/B 列名与字典完全一致
   - `A ⋈ B on (pid, day_num, row_id)` 行数不变（守住 S4）
   - `waso_computed_minutes == awake_getup_diff_h × 60`（守住单位换算）
5. 真实数据跑通，人眼看 A 的前几行
6. S2 图标题（独立，随时可做）
7. **S5：对参照实现的算式快照** —— B1/B2 已定案，可以做了。
   建议放最后，与 `finalize_columns()` 一起加

### 待办的外部依赖

- [ ] **向 Maia 确认问卷原题**（见 §一）。这是本轮唯一无法内部解决的问题

### 未处理项

详见 `work_logs/open_issues_2026-08-07.md` 的 🟡 中等 与 ⚪ 已知债 两节：
M1（sleeponset 别名，B1 定案后仍存在）、M4（物质摄入时刻）、M5（`StartDate` 类型）、
M7（`sleepefficiency_percent` 名实不符）、M8（两个 TST / 两个 Clean 口径）、
D1（`output/` 仍全局共享）、D3（`run.sh` 两个毛病）、D4（Δ 渲染）、
D5（🟡 三项 NULL 加固）、D6（配置死代码）、D7（`proj_splclean` 归档）。

---

## 六、方法备注

本轮审计的价值不在于我最初提出的那些命名问题，而在于**对照了参照实现**。

`verify_v1_3_snapshot.R` 比对的是「splsleep 旧路径 vs splsleep 的 S3 链」，
证明的是重构没改变结果——**它挡不住算法偏离参照实现**，
因为 B1 的偏离在 snapshot 建立之前就存在，两边都带着它。

**教训：内部一致性测试与外部保真度测试是两回事，都需要。**
S5 补的正是后者。

---

> 2026-08-09 | 对照 commit `610572f` | 待 Maia 确认问卷原题
