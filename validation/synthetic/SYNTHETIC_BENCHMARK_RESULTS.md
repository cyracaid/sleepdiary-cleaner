# Synthetic Benchmark — Calibrated Injected-Error Validation

Status: **complete** (2026-08-16). The first-pass harness (08-12) produced
the two v1.4.1 bug fixes; the full design has since been executed — pooled
recall/specificity with cluster-bootstrap CIs, L2 type-correct tier,
FAR_flag/MRR/magnitude, multiverse analysis, leave-one-out ablation, control
baselines, seed sensitivity, and FDR correction. See
`validation/synthetic/results/` for the full result set and the README
[Validation](https://github.com/cyracaid/sleepdiary-cleaner#validation)
section for the 8-step walkthrough. This file documents the original first
pass and the patch that fixed the two bugs it found.

Scripts and data live in `validation/synthetic/` (gitignored `.rds`/`.csv`
outputs are regenerated by running the scripts; commit only tracks the code,
the error taxonomy, and the aggregate result tables below).

## What's here

- `generate_clean_data.R` — parameterized clean-data generator. `mode=pure`
  (near-zero-variance, for the false-correction test) vs `mode=plausible`
  (population-realistic: healthy_adult, insomnia_like, short_sleeper,
  shift_like presets). Every row is monotonic and format-valid by
  construction.
- `error_catalog.yaml` — 12-category error taxonomy, each tagged with
  provenance (observed / literature / blind_spot) so the injected
  distribution isn't circularly calibrated off the pipeline's own detected
  errors.
- `inject_errors.R` — participant-clustered injector (not IID), enriches
  each category to a fixed target count, logs one ground-truth row per
  injected error (original value, corrupted value, true value, mechanism).
- `evaluate_fcr.R` — false-alteration-rate evaluator (structurally-pure and
  population-stratified inputs).
- `evaluate_detection.R` — per-category detection/recall evaluator, compares
  the pipeline's derived values directly against `ground_truth.csv`'s
  `true_value` column.
- `results/` — aggregate result tables, including
  `recall_specificity_ci.csv` (pooled recall 0.995 [0.993, 0.997],
  specificity 1.0), `far_flag_alter.csv` (0/1,609 both), `fcr_pure_n10000_result.csv`,
  `control_baselines.csv` (0 / 0.623 / 0.995), `mrr_magnitude.csv` (MRR 0),
  `l2_tier.csv`, `results/multiverse/` (spec curve, variance decomposition
  D2 = 44%, instability), `downstream_sensitivity.csv`,
  `leave_one_out.csv`, `seed_sensitivity.csv`, `fdr_corrected.csv`.

## First-pass results (2026-08-12, kept for provenance)

### FCR test A — structurally pure, n=10,000

**FAR_alter = 0/10,000.** Rule-of-three 95% upper bound ≈ 0.03%. Verified
against the pipeline's own parsed-truth columns (not raw display strings,
which would have falsely counted 10,000/10,000 "alterations" that were pure
reformatting).

### Test B — physiologically-realistic per population, n=3,000 × 4

Zero value alterations confirmed on all 12,000 rows. Flag rate by
population:

| Population | n | Error | Unusual | DURATION_ISSUE | Flag rate |
|---|---|---|---|---|---|
| healthy_adult | 3000 | 0 | 0 | 0 | 0.0% |
| short_sleeper | 3000 | 0 | 0 | 0 | 0.0% |
| shift_like | 3000 | 9 | 1 | 0 | 0.3% |
| insomnia_like | 3000 | 0 | 6 | 224 | 7.7% |

Quantifies `THRESHOLDS.md`'s documented caveat that default thresholds are
tuned on a healthy young-adult sample: a legitimately-long-SOL/WASO
population gets flagged at roughly 26x the rate of the tuning population,
on real, unmanipulated (synthetic-clean) data. Zero of these flags
corresponded to a silently-altered value. (Note: the per-population
FAR_alter split still needs an NA-vs-altered fix in the evaluator before an
exact number is quoted per population — the pipeline's own "Corrected: +0"
report is the trustworthy figure here.)

### Detection/recall — n=7,000 (4,736 injected across 11 categories + 336 cross-participant + 2,264 clean control)

| Category | n | Correct | Flagged (unresolved) | Misrepaired | Missed |
|---|---|---|---|---|---|
| format_no_colon | 400 | 100.0% | 0 | 0 | 0 |
| format_malformed_colon | 400 | 100.0% | 0 | 0 | 0 |
| implausible_duration | 400 | 0% (100% flagged) | 400 | 0 | 0 |
| adjacent_swap_sleep_awake | 400 | 0% (100% flagged) | 400 | 0 | 0 |
| adjacent_swap_bed_sleep | 400 | 100.0%† | 0 | 0 | 0 |
| adjacent_swap_awake_getup | 400 | 100.0%† | 0 | 0 | 0 |
| ampm_swap | 400 | 50.8% | 49.2% | 0 | 0 |
| compound_ampm_and_swap | 400 | 33.0% | 67.0% | 0 | 0 |
| mmss_confusion | 400 | 94.8% | 2.8% | 2.5% | 0 |
| cross_participant_spike | 336 | — | 88.7% | 11.3%‡ | 0 |
| **field_misentry_sol** (pre-patch) | 400 | 4.2% | 0 | **95.8%** | 0 |
| **field_misentry_waso** (pre-patch) | 400 | 3.0% | 1.0% | **96.0%** | 0 |

† 378/378 and 323/323 on the genuinely-corrupted subset once no-op ties
(original==true value) are excluded — cross-checked against the pipeline's
own `correction_type` audit column, not just the evaluator's read.
‡ flag-only reading (v1 evaluator); superseded by the current-run numbers in
`recall_specificity_ci.csv` (L1 0.907).

**Headline finding:** field misentry (a clock time typed into the SOL/WASO
duration field — an observed error pattern in real data, see
`error_catalog.yaml`'s design rationale) doesn't just evade detection, it
gets silently "corrected" to a wrong value 95.8–96.0% of the time with no
flag. This is what `checkforerrors_processing.R`'s Part A4 patch (shipped
in v1.4.1) fixes.

### Patch verification (post-fix, same harness)

| | Pre-patch | Post-patch |
|---|---|---|
| field_misentry_sol silent misrepair | 383/400 (95.8%) | **14/400 (3.5%)** |
| field_misentry_sol overall caught | 17/400 (4.2%) | **386/400 (96.5%)** |
| field_misentry_waso silent misrepair | 384/400 (96.0%) | **0/400 (0%)** |
| field_misentry_waso overall caught | 16/400 (4.0%) | **400/400 (100%)** |
| FCR n=10,000 false positives from the new rule | n/a | 0/10,000 |

WASO closed completely. SOL's residual 3.5% are "01:XX"-shaped clock times
that produce no `_correctionsmade` text at all — disclosed as a structural
limit (no textual signal to key off), not rounded up to 100%. Full
`testthat` suite unchanged before/after (same 4 pre-existing unrelated
failures, 0 new).

A second, independent bug was found while building the harness: the
human-review CSVs (`generate_correction_files.R`, Section 13) were never
actually being written — the `write.csv()` calls were commented out while
the pipeline log unconditionally printed "Files saved." Also fixed in
v1.4.1.

### Addendum (2026-09-02): the "Patch verification" numbers above were never
### backed by a re-run CSV — now they are, and the real breakdown is different

The "Patch verification" table above was hand-derived at the time (silent
misrepair count + overall-caught count from an ad-hoc check), never from a
regenerated `results/*.csv`. `detection_outcomes_v4.csv` — the one CSV this
whole doc's earlier table is built from — is genuinely the **first-pass,
pre-patch** run (its own row labels above say so), and nothing ever
replaced it with a post-patch equivalent. Separately, `error_catalog.yaml`
and `ground_truth_enrichment.csv` grew two more categories
(`adjacent_swap_large_gap_left_clean`, `sol_window_contradiction`) and
`cross_participant_spike`/`no_error_control`'s injected counts changed
after that CSV was written, so it was stale for reasons beyond the patch
too.

Re-ran the full harness 2026-09-02 against the currently-installed
`sleepcleanr` 1.4.5 (Part A4 patch included): `run_one.R` on the full,
current `corrupted_enrichment.rds` (n=7,000) → `evaluate_detection.R`
(v4) against the current `ground_truth_enrichment.csv` → written to
`results/detection_outcomes_v4_current.csv` (the original
`detection_outcomes_v4.csv` is left untouched as the pre-patch record cited
above). 6 of 15 categories matched the original file exactly
(`adjacent_swap_time_awake_am_time_getup_am`,
`adjacent_swap_time_bed_am_time_sleep_am`,
`adjacent_swap_time_sleep_am_time_awake_am`, `format_malformed_colon`,
`format_no_colon`, `implausible_duration` — all fully deterministic,
non-random categories), which is the evidence that this re-run is a
faithful reproduction of the same harness, not a different setup producing
different numbers by accident.

**field_misentry, real current breakdown:**

| | field_misentry_sol | field_misentry_waso |
|---|---|---|
| CORRECT (auto-fixed to the true value) | 7/400 (1.8%) | 13/400 (3.2%) |
| FLAGGED_UNRESOLVED (caught, routed to human review) | 393/400 (98.2%) | 387/400 (96.8%) |
| MISREPAIRED (silently changed to a wrong value) | **0/400 (0%)** | **0/400 (0%)** |

The core claim holds and is now stronger than originally stated: the
silent-misrepair bug Part A4 targets is fully closed for both fields (0%,
not the previously-claimed residual 3.5% for SOL — the "01:XX" edge case
described above no longer reproduces on this run; worth a closer look at
whether it was fixed by a later, unrelated change, but not re-investigated
here). **However**, "overall caught 96.5%/100%" in the table above is true
only under a looser definition of "caught" that counts
`FLAGGED_UNRESOLVED` together with `CORRECT` — almost none of that 96.5%/
100% is the pipeline actually landing on the right value by itself (1.8%/
3.2% are); the patch's real effect is converting a silent wrong-answer into
a human-review flag, not into an automatic fix. Both are legitimate, safe
outcomes for a data-cleaning pipeline, but they are different claims, and a
reader skimming "overall caught: 96.5%" next to "silently corrected to a
wrong value" framing would reasonably assume the former means
auto-corrected. Left both tables above as originally written (development
history), rather than edited after the fact, per this project's own
provenance-preservation practice — but treat `detection_outcomes_v4_
current.csv` and this addendum, not the tables above, as the current
description of pipeline behavior.

See `2026-09-04_work_log.md` for the fuller trail (why this was checked,
the two-stage discovery, full per-category diff against the original CSV).

## What's solid vs. what needs another pass

**Solid:** FCR test A (0/10,000 on parsed-truth values); Test B population
flag-rate gradient; format_no_colon/malformed_colon (100%);
field_misentry_sol/waso pre- and post-patch numbers; ampm_swap (0%
misrepair/miss); adjacent_swap_bed_sleep/awake_getup (100% on genuine
subset); compound_ampm_and_swap (0% misrepaired/missed, flagged rows
verified to reach the delivered dataset); the `generate_correction_files.R`
logging bug (root-caused, patched, reverified not to interfere with Step 6).

**Completed since first pass (2026-08-16):** pooled recall/specificity with
participant-level cluster-bootstrap CIs (`recall_specificity_ci.csv`);
L2 type-correct tier (`l2_tier.csv`); FAR_flag/MRR/magnitude
(`far_flag_alter.csv`, `mrr_magnitude.csv`); control baselines
(`control_baselines.csv`); multiverse + variance decomposition + instability
(`results/multiverse/`); leave-one-out ablation (`leave_one_out.csv`, flag-
based definition — reported as supplementary with a footnote); downstream
sensitivity (`downstream_sensitivity.csv`); seed sensitivity
(`seed_sensitivity.csv`); FDR-corrected per-category table
(`fdr_corrected.csv`). Known-weak family: `cross_participant_spike` (L1
0.886–0.907 across seeds; value-correct 0 by design — audit-only family).
